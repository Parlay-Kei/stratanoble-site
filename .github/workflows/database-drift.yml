name: Database Drift Detection

on:
  pull_request:
    paths:
      - 'supabase/migrations/**'
      - 'database/**'
  push:
    branches: [main]
    paths:
      - 'supabase/migrations/**'
      - 'database/**'

jobs:
  validate-migrations:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '22.x'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Setup Supabase CLI
      uses: supabase/setup-cli@v2
      with:
        version: 2.116.0

    - name: Validate migration files
      run: |
        echo "🔍 Validating migration file structure..."

        # Check that migrations are properly numbered and named
        # Skip files that start with APPLY_VIA_SQL_EDITOR (manual scripts)
        for file in supabase/migrations/*.sql; do
          filename=$(basename "$file")

          # Skip manual SQL editor scripts
          if [[ "$filename" =~ ^APPLY_VIA_SQL_EDITOR ]]; then
            echo "⏭️  Skipping manual script: $filename"
            continue
          fi

          if [[ ! "$filename" =~ ^[0-9]+_[a-z_]+\.sql$ ]]; then
            echo "❌ Invalid migration filename: $filename"
            echo "   Expected format: 0001_description.sql or 20260101_description.sql"
            exit 1
          fi
        done

        echo "✅ All migration files follow naming convention"

    - name: Check SQL syntax
      run: |
        echo "🔍 Basic SQL syntax validation..."
        
        for file in supabase/migrations/*.sql; do
          echo "Checking $(basename "$file")..."
          
          # Basic syntax checks
          if grep -q "CREATE TABLE.*(" "$file" && ! grep -q "IF NOT EXISTS" "$file"; then
            echo "⚠️  Warning: $(basename "$file") has CREATE TABLE without IF NOT EXISTS"
          fi
          
          if grep -q "INSERT INTO" "$file" && ! grep -q "ON CONFLICT" "$file"; then
            echo "⚠️  Warning: $(basename "$file") has INSERT without conflict handling"
          fi
        done
        
        echo "✅ SQL syntax validation completed"

    - name: Start Supabase (if Docker available)
      run: |
        if command -v docker &> /dev/null; then
          echo "🐳 Docker found, starting Supabase..."
          supabase start
          
          echo "🔍 Running db diff to check for drift..."
          supabase db diff > diff_output.txt || true
          
          if [ -s diff_output.txt ]; then
            echo "❌ Database drift detected:"
            cat diff_output.txt
            exit 1
          else
            echo "✅ No database drift detected"
          fi
          
          supabase stop
        else
          echo "⚠️  Docker not available in CI, skipping Supabase tests"
          echo "   Validating migrations structurally instead..."
          node scripts/combine-migrations.js
          echo "✅ Migrations combined successfully"
        fi

    - name: Verify migration order
      run: |
        echo "🔗 Checking migration dependencies..."
        
        prev_num=""
        for file in supabase/migrations/*.sql; do
          filename=$(basename "$file")
          num=$(echo "$filename" | cut -d'_' -f1)
          
          if [[ -n "$prev_num" && "$num" -lt "$prev_num" ]]; then
            echo "❌ Migration numbering error: $filename should come after previous migration"
            exit 1
          elif [[ -n "$prev_num" && "$num" -eq "$prev_num" ]]; then
            echo "⚠️  Warning: duplicate prefix $num in $filename — consider renaming"
          fi
          
          prev_num="$num"
        done
        
        echo "✅ Migration order is correct"

    - name: Check for sensitive data
      run: |
        echo "🔍 Checking for sensitive data in migrations..."
        
        # Narrow check: assignment patterns suggesting hardcoded secrets, not SQL schema keywords
        if grep -r -E -i "(password|secret|api_key|auth_token)\s*=\s*'[^']{8,}'" supabase/migrations/ --include="*.sql"; then
          echo "❌ Potential hardcoded secret found in migrations"
          echo "   Use environment variables or vault references instead"
          exit 1
        fi
        
        echo "✅ No sensitive data detected"

    - name: Archive combined migrations
      uses: actions/upload-artifact@v7
      if: always()
      with:
        name: combined-migrations
        path: database/combined_migrations.sql
        retention-days: 30