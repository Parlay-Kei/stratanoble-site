[build]
  command = "rm -rf apps/achievery-mobile && npm install --legacy-peer-deps && npm run build --workspace=@strata-noble/website"
  publish = "apps/website/.next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20.18.0"
  # Ensure Netlify Next.js plugin is NOT skipped
  NETLIFY_NEXT_PLUGIN_SKIP = "false"
  # Skip Supabase CLI binary download in production builds
  SKIP_DOWNLOAD = "true"

# Security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "geolocation=(), microphone=(), camera=()"
