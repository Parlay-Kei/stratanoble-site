[build]
  command = "cd apps/website && npm install --legacy-peer-deps && npm run build"
  publish = "apps/website/.next"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

[[plugins]]
  package = "@netlify/plugin-nextjs"
