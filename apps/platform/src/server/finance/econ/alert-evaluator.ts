/**
 * Alert Evaluator (Skill S5)
 *
 * Evaluate alerts against snapshots using ruleset thresholds.
 */

// Re-export from skills for backward compatibility
export { evaluateAlerts } from './skills';
